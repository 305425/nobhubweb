{
  "routes": [
    {
      "method": "GET",
      "path": "/menubars",
      "handler": "menubar.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/menubars/count",
      "handler": "menubar.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/menubars/:id",
      "handler": "menubar.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/menubars",
      "handler": "menubar.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/menubars/:id",
      "handler": "menubar.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/menubars/:id",
      "handler": "menubar.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
